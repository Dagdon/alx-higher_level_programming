#!/usr/bin/node
/* print a three line message using an array of loop */

const array = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (let i = 0; i < array.length; i++) {
  console.log(array[i]);
}
